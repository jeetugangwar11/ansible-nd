[defaults]
collections_path = /Users/jeeram/ansible/collections
host_key_checking = False
# macOS fork() crash fix: "+[NSCharacterSet initialize] may have been in progress
# in another thread when fork() was called"
# OBJC_DISABLE_INITIALIZE_FORK_SAFETY must be set before ansible forks workers.
# Run via: OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES ansible-playbook ...
# or source this file's companion: source set_env.sh
